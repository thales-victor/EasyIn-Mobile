import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  form: {
    marginTop: -30,
    alignItems: 'center',
    paddingHorizontal: 50
  },
  footer: {
    marginBottom: 40
  }
});