import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  form: {
    alignItems: 'center',
    paddingHorizontal: 50,
    marginTop: 30,
  },
  footer: {
    width: '100%',
    marginBottom: 150
  }
});